version: '3.8'

services:
  backend:
    build: ./api-python
    ports:
      - "8000:8000"
    volumes:
      - ./api-python:/app
    environment:
      - DATABASE_URL=sqlite:///./ativos.db

  frontend:
    build: ./dashboard-ativos
    ports:
      - "80:80"
    depends_on:
      - backend